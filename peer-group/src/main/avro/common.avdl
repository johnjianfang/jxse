@namespace("org.apache.peer.protocol")
protocol Common {
    record Peer {
        string pid;
        string address;
    }

    record Payload {
        long sequ;
        bytes data;
        long crc;
    }
}